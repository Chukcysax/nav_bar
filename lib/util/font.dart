import 'package:flutter/material.dart';

Text styleSheet(String text, String fontFamily, double fontSize,
    FontWeight fontWeight, Color color) {
  return styleSheet(text, fontFamily, fontSize, fontWeight, color);
}
